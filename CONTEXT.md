# Organization context

`.github` is GitHub's special organization repository. It owns public profile text, branding and security metadata.

Foundation owns shared Mardwerk terms and boundaries. Towerright owns the private production platform. Lower repositories own their Tools and code.

Public contributors must use the contributor documentation in the repository they change. No contribution should require access to a private repository.
